pre-commit:
  commands:
    fix:
      glob: "**/*.{ts,tsx}"
      run: bunx biome check --write {staged_files}
      stage_fixed: true
    tsc:
      glob: "**/*.{ts,tsx}"
      run: bunx tsc --noEmit {staged_files}

skip_output:
  - execution_out
