{
    acme_ca https://acme.zerossl.com/v2/DV90
    email admin@asius.ai
}

ssh.asius.ai {
    # Redirect root to docs
    redir / https://asius.ai/docs/ssh permanent

    # Proxy everything else (health, WebSocket)
    reverse_proxy localhost:8080
}
